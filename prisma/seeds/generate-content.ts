import { generateContent } from './content';

// Export for use in seed files
export { generateContent };

// Export utility functions for individual content generation if needed
export * from './content';
